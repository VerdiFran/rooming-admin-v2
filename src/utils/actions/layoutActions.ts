import {Action} from "./Action";

/**
 * Get information about layout action.
 */
export const GET_LAYOUT_INFO_ACTION: Action = {
    title: 'Подробнее',
    type: 'GET-INFO-ACTION'
}

/**
 * Add layout to session action.
 */
export const ADD_TO_SESSION: Action = {
    title: 'Добавить в сессию',
    type: 'ADD-TO-SESSION'
}